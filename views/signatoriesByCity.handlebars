{{!-- 

#1
dynamic new template
MORE abitious: make the other signatories page dynamic -> need quite some if else statements

#2
New Query
only need to add in the end WHERE city = "$1", e.g. $1 = Berlin etc.-> restricting condition on which we join tables

#3
Pitfall
#a when inserted into db: .tolowercase -> OR integrated function in postgres LOWER(profiles.city) = LOWER("$1") (optionally add a plugin that deletes special characters)
#b when retrieved: we have to do something else

#4 
Dynamic route
last bit of URL placeholder

app.get("/signer/city/:cityName", (req, res) => {
const cityName = req.params.cityName })

if(cityName.toLowerCase != cityName) {
res.redirect("/test" + cityName.toLowerCase())
}

again: problem with strange capitalisation (if we used .tolowercase )
 --}}